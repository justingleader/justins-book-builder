\begin{tcolorbox}[
  colframe={{ 
    type == 'warning' ? 'orange' : 
    type == 'danger' ? 'red' : 
    type == 'success' ? 'green' : 'blue' 
  }},
  colback={{ 
    type == 'warning' ? 'orange!5' : 
    type == 'danger' ? 'red!5' : 
    type == 'success' ? 'green!5' : 'blue!5' 
  }},
  title={{ title }},
  fonttitle=\bfseries
]
{{ content }}
\end{tcolorbox} 